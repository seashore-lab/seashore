# 客户端工具

某些工具在**客户端**（浏览器/应用程序）而不是服务器上执行最安全：

- 文件系统操作
- 特权用户数据访问
- 支付操作

在这些情况下：

1. 让智能体*提议*工具调用。
2. 向用户展示请求。
3. 在客户端执行。
4. 将结果作为 `tool` 消息发回。

Seashore 的流事件（`tool-call-start` / `tool-call-args` / `tool-call-end`）旨在支持此模式。

如果您需要服务器端工具的显式审批流程，请参阅[工具审批](./approval.md)。
