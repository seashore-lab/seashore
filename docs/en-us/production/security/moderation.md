# Content Moderation

Moderation can be implemented as:

- rule-based checks (keywords, regex)
- LLM-based classification
- external API integration

Example 07 demonstrates a custom rule that simulates an external moderation API.
