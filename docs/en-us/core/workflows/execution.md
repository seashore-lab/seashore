# Workflow Execution

Two main modes:

## `execute()`

Runs the full workflow and returns a result object you can query for node outputs.

## `stream()`

Streams events while the workflow runs.

The example [Example 03](../../examples/03-basic-workflow.md) shows both.
